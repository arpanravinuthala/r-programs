print("enter the elements")
x<-scan()
sum=0
for(i in x)
{
  sum=sum+i
}
l=length(x)
cat("mean of the given data:",sum/l)print("enter the elements")
x<-scan()
sum=0
for(i in x)
{
  sum=sum+i
}
l=length(x)
cat("mean of the given data:",sum/l)
